import React, { useState } from "react";

const ProductMoreInfo = () => {
  const [active, setActive] = useState(1);
  return <div className="bg-[#cdcaca]">ProductMoreInfo</div>;
};

export default ProductMoreInfo;
